FUNCTION AGUINALD
SELECT RECIBOS
M.FE_MENOS = CTOD("01/" + TRANSFORM(MONTH(DATE())-6,"@L 99") + "/" + TRANSFORM(YEAR(DATE() - 180), "@L 9999"))
maximo = 0
CALCULATE MAX(TOTHABERES + TOTADICION) FOR COD_EMPADO = EMPLEADO.CODIGO .AND. FECHA > M.FE_MENOS TO maximo
select concepto
RETURN maximo / 2